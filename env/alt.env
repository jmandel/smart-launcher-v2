# Example environment overrides for the local SMART launcher stack
# Copy and adjust these values or pass the file directly to docker compose:
# docker compose --env-file env/local.env up -d

LAUNCHER_PORT=9090
HAPI_PORT=8181
POSTGRES_PORT=5532

FHIR_SERVER_R4=http://hapi:8181/fhir
PICKER_ORIGIN=/patient-browser
